{
  "name": "navio-blsct",
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/build.js && node-gyp configure build && echo 'Done.'",
    "test": "node src/test.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.0.5",
    "ts-jest": "^29.4.0"
  },
  "dependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3"
  }
}
