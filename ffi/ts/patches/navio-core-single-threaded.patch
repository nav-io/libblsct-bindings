diff --git a/src/blsct/arith/blst/blst_scalar.cpp b/src/blsct/arith/blst/blst_scalar.cpp
index 500f357aa4..33342e2dc9 100644
--- a/src/blsct/arith/blst/blst_scalar.cpp
+++ b/src/blsct/arith/blst/blst_scalar.cpp
@@ -25,7 +25,7 @@
 #include <cerrno>
 #include <fcntl.h>
 #include <unistd.h>
-#if defined(__APPLE__) || defined(__OpenBSD__) || defined(__FreeBSD__) || defined(__NetBSD__) || (defined(__GLIBC__) && (__GLIBC__ > 2 || (__GLIBC__ == 2 && __GLIBC_MINOR__ >= 25)))
+#if defined(__APPLE__) || defined(__OpenBSD__) || defined(__FreeBSD__) || defined(__NetBSD__) || defined(__EMSCRIPTEN__) || (defined(__GLIBC__) && (__GLIBC__ > 2 || (__GLIBC__ == 2 && __GLIBC_MINOR__ >= 25)))
 #include <sys/random.h>
 #define BLST_SCALAR_HAVE_GETENTROPY 1
 #endif
diff --git a/src/blsct/arith/blst/blst_util.cpp b/src/blsct/arith/blst/blst_util.cpp
index acb6ce987b..8e7b9c9623 100644
--- a/src/blsct/arith/blst/blst_util.cpp
+++ b/src/blsct/arith/blst/blst_util.cpp
@@ -40,10 +40,14 @@ BlstG1Point BlstUtil::MSM(const BlstG1Point* pts, const BlstScalar* scalars, siz
 {
     if (n == 0) return BlstG1Point();
     if (threads == 0) threads = DefaultThreads();
+#ifdef WASM_SINGLE_THREADED
+    threads = 1; // no pthreads in the WASM build
+#else
     if (threads == 0) {
         threads = std::thread::hardware_concurrency();
         if (threads == 0) threads = 1;
     }
+#endif
 
     // blst's MSM takes affine points and canonical little-endian scalars.
     // The Jacobian -> affine conversion is a batched inversion; mcl's mulVec
diff --git a/src/blsct/public_keys.cpp b/src/blsct/public_keys.cpp
index a45f228c9d..285775234f 100644
--- a/src/blsct/public_keys.cpp
+++ b/src/blsct/public_keys.cpp
@@ -66,7 +66,11 @@ bool PublicKeys::CoreAggregateVerify(const std::vector<PublicKey::Message>& msgs
     // accumulated in per-thread pairing contexts (hash-to-G2 + Miller loop
     // dominate and parallelise perfectly), merged, then one final
     // exponentiation. An identity public key fails (BLST_PK_IS_INFINITY).
+#ifdef WASM_SINGLE_THREADED
+    size_t threads = 1; // no pthreads in the WASM build
+#else
     size_t threads = std::thread::hardware_concurrency();
+#endif
     if (threads == 0) threads = 1;
     threads = std::min(threads, n);
 
diff --git a/src/blsct/range_proof/bulletproofs/range_proof_logic.cpp b/src/blsct/range_proof/bulletproofs/range_proof_logic.cpp
index 0a6142dada..ac3f3c5dca 100644
--- a/src/blsct/range_proof/bulletproofs/range_proof_logic.cpp
+++ b/src/blsct/range_proof/bulletproofs/range_proof_logic.cpp
@@ -257,7 +257,14 @@ bool RangeProofLogic<T>::VerifyProofs(
 
     // Launch a verification task for each proof transcript in parallel
     for (const RangeProofWithTranscript<T>& p : proof_transcripts) {
-        futures.emplace_back(std::async(std::launch::async, [this, &p, max_mn]() -> bool {
+#ifdef WASM_SINGLE_THREADED
+        // WASM build without pthreads: run the task on the calling thread when
+        // its future is consumed instead of spawning a worker.
+        constexpr auto launch_policy = std::launch::deferred;
+#else
+        constexpr auto launch_policy = std::launch::async;
+#endif
+        futures.emplace_back(std::async(launch_policy, [this, &p, max_mn]() -> bool {
             if (p.proof.Ls.Size() != p.proof.Rs.Size()) return false;
 
             const range_proof::Generators<T> gens = m_common.Gf().GetInstance(p.proof.seed);
diff --git a/src/blsct/range_proof/bulletproofs_plus/range_proof_logic.cpp b/src/blsct/range_proof/bulletproofs_plus/range_proof_logic.cpp
index 089cd96c28..5773daa596 100644
--- a/src/blsct/range_proof/bulletproofs_plus/range_proof_logic.cpp
+++ b/src/blsct/range_proof/bulletproofs_plus/range_proof_logic.cpp
@@ -573,7 +573,14 @@ bool RangeProofLogic<T>::VerifyProofs(
     // its own std::async thread, so a batch of n proofs uses up to n cores
     // with no nested threading (BlstUtil::MSM's own tiling stays off unless
     // BlstUtil::SetDefaultThreads is raised).
-    if (proof_transcripts.size() <= 1) {
+#ifdef WASM_SINGLE_THREADED
+    // WASM build without pthreads: std::async(launch::async) cannot spawn a
+    // thread, so verify every proof sequentially on the calling thread.
+    constexpr bool single_threaded = true;
+#else
+    constexpr bool single_threaded = false;
+#endif
+    if (single_threaded || proof_transcripts.size() <= 1) {
         for (size_t idx = 0; idx < proof_transcripts.size(); ++idx) {
             if (!verify_one(idx)) return false;
         }
@@ -731,7 +738,11 @@ AmountRecoveryResult<T> RangeProofLogic<T>::RecoverAmounts(
         }
     };
 
+#ifdef WASM_SINGLE_THREADED
+    size_t nthreads = 1; // no pthreads in the WASM build
+#else
     size_t nthreads = std::thread::hardware_concurrency();
+#endif
     if (nthreads == 0) nthreads = 1;
     nthreads = std::min(nthreads, n);
     if (nthreads <= 1) {
